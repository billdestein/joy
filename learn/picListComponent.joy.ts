//----------------------------------------------------------------------------------------------------
// picListComponent
//----------------------------------------------------------------------------------------------------
export const picListComponent = `

The PicListComponent has a single prop, workbook, of type WorkbookType.

The PiclistComponent iterates over the pics in the input workbook.  For each pic,
a PicComponent is rendered.  The pic.filename is passed as the 'name' prop
of the PicComponent.

`
